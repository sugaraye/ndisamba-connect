NDI SAMBA - Next.js PRO (converted)

Install:
  npm install
  # generate Tailwind if used:
  # npx tailwindcss -i ./app/input.css -o ./app/output.css --watch

Run:
  npm run dev

Replace:
  - public/assets/* with real photos (filenames kept)
  - set BOT_TOKEN in Vercel env if deploying webhook

